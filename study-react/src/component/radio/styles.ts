export const radioStyles = (theme) => ({
    root: {
        width: "300px",
        padding: "20px",
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "flex-start",
        fontSize: "16px"
    },
    label: {
        color: theme.color_radio
    }
});
